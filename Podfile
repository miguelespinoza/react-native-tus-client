platform :ios, '8.0'

target 'YourAppTarget' do
  use_frameworks!

  # Include the React Native Core
  pod 'React-Core'

  # Point to your custom fork of TusKit
  pod 'TusKit', :git => 'https://github.com/miguelespinoza/TusKit.git', :branch => '1.4.2-iOS18-patch'
end
